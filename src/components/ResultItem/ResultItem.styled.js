import styled from 'styled-components';

export const ResultItemWrapper = styled.div`
    margin: 0.8rem 0;
    padding: 1rem;
    background-color: #FFF;
    border: 1px solid #000;
    border-radius: 4px;
    cursor: pointer;
`;

export const ResultItemSection = styled.div`
    margin: 0.2rem;
`;

export const ResultItemLabel = styled.h6`
    color: #000;
    text-transform: uppercase;
    font-size: 1rem;
`;

export const ResultItemText = styled.p`
    color: gray;
    text-transform: uppercase;
`;